# خطة الأمان

## المصادقة

- تخزين كلمات المرور باستخدام Argon2id.
- جلسات موقعة داخل HttpOnly cookies.
- عدم تخزين session token الخام في قاعدة البيانات، بل hash فقط.
- مدة الجلسة الافتراضية 8 ساعات.
- `Secure` cookies في الإنتاج.

## الصلاحيات

- RBAC مركزي للأدوار:
  `SUPER_ADMIN`, `GOVERNORATE_ADMIN`, `OPERATIONS_MANAGER`, `DISTRIBUTION_ADMIN`, `STATION_MANAGER`, `STATION_EMPLOYEE`, `TANKER_OPERATOR`, `CITIZEN`.
- كل API حساس يتحقق من الصلاحية server-side.
- لا تعتمد الواجهة على إخفاء الأزرار كحماية.

## سجل التدقيق

- تسجيل الدخول الناجح والفاشل.
- إنشاء المستخدمين وتغيير الصلاحيات.
- تفعيل وضع الأزمة وتغيير القواعد.
- تغييرات المخزون.
- تجهيز الوقود ورفض التجهيز.
- إجراءات الصهاريج والشحنات.
- الوصول الإداري الحساس لبيانات المواطنين عند الحاجة.

## حماية البيانات

- Data minimization.
- Masking لأرقام الهواتف في الواجهات غير المخولة.
- QR لا يحتوي معلومات حساسة؛ يحتوي معرفا أو توكنا آمنا فقط.
- الأسرار في `.env` فقط، وملف `.env.example` بدون أسرار.

## الحماية التشغيلية

- Rate limiting لمحاولات الدخول.
- Validation عبر Zod.
- Prisma لحماية SQL injection.
- Security headers في Next middleware.
- HSTS في الإنتاج.
- CSRF للطلبات التي تعتمد على cookies عند توسيع العمليات الحساسة.

## ضعف الإنترنت

- لا يتم تأكيد أي عملية تجهيز إلا بعد تأكيد السيرفر.
- عند فقد الاتصال تعرض الواجهة حالة واضحة.
- لا يوجد Offline Sync للتجهيز في النسخة الأولى حتى لا يحدث double dispensing.
