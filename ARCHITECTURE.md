# Architecture - Salah Al-Din Smart Fuel Management System

## Runtime

```
Browser / PWA
  -> Next.js UI
  -> Next.js API Route Handlers
  -> Domain services in src/lib
  -> Prisma ORM
  -> PostgreSQL
```

## طبقات النظام

- `src/app`: صفحات و APIs.
- `src/components`: مكونات الواجهة القابلة لإعادة الاستخدام.
- `src/lib`: منطق RBAC، المصادقة، التدقيق، قاعدة البيانات، والتحقق.
- `prisma`: schema و migrations و seed التطوير لاحقا.
- `tests`: اختبارات وحدات للمنطق الحرج.

## حدود Phase 1

Phase 1 لا تنفذ توزيع الوقود بعد. هي تؤسس:

- هوية النظام.
- تسجيل الدخول والخروج.
- صلاحيات مركزية.
- سجل تدقيق.
- schema جاهز للتوسع.
- واجهة حكومية RTL مع Empty States حقيقية.

## حدود Phase 2

Phase 2 تضيف سطح المحطات والمخزون بدون تشغيل صرف الوقود بعد:

- إدارة المحطات وأنواع الوقود عبر APIs محمية.
- رصيد مخزون لكل محطة ونوع وقود.
- دفتر حركة مخزون يسجل `before/change/after`.
- التعديل اليدوي محصور بأدوار عالية ويحتاج سببا إلزاميا.

## حدود Phase 3

Phase 3 تضيف تسجيل المواطن والمركبة وQR:

- تسجيل المالك برقم الهاتف.
- تخزين بيانات اللوحة بشكل مرن يدعم اختلاف الصيغ العراقية.
- إنشاء Vehicle ID داخلي.
- QR payload عشوائي لا يحمل الاسم أو الهاتف أو رقم اللوحة.
- تخزين hash سر QR فقط والتحقق منه من السيرفر.

## حدود Phase 4

Phase 4 تضيف وضع الأزمة والتخصيص:

- قاعدة أزمة قابلة للتعديل تشمل نوع الوقود، المحطات، أنواع المركبات، الحصة، السعة، وبداية/نهاية القرار.
- Time Slots تنشأ من القاعدة ولا تعتمد على قيم ثابتة.
- Allocation Engine لا يستخدم AI لاتخاذ قرار حساس، بل قواعد وسعة متاحة.
- منع Overbooking يتم بتحديث شرطي داخل transaction.

## قرارات مهمة

- لا توجد بيانات إنتاج وهمية.
- بيانات التطوير، عند إضافتها لاحقا، تكون باسم واضح `DEVELOPMENT ONLY`.
- لا توجد AI decisions لصرف الوقود.
- كل قرارات الحصص والقواعد ستكون قابلة للتعديل في قاعدة البيانات في المراحل التالية.
